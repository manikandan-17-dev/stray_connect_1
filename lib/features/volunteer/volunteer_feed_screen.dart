import 'package:flutter/material.dart';

class VolunteerFeedScreen extends StatelessWidget {
  const VolunteerFeedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Volunteer Feed (stub)'),
    );
  }
}
