import 'package:flutter/material.dart';

class VetIntakeScreen extends StatelessWidget {
  const VetIntakeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vet Intake (stub)'),
    );
  }
}
